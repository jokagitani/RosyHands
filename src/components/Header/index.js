import React from 'react';
import './style.scss';

const Header = props => {
    return(
        <header className="header">
            <h1 className="h2">
                Rosy Hands
            </h1>
        </header>
    );
};

export default Header;
